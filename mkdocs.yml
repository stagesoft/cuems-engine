site_name: CUEMS Engine - FormitGo
repo_url: https://github.com/cuems/cuems-engine
theme:
  name: material

markdown_extensions:
  - admonition

plugins:
- search
- mkdocstrings:
    default_handler: python
    handlers:
      python:
        paths: [src]
