[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cuemsengine"
dynamic = ["version"]
description = "Engine infraestructure of the CueMS system"
readme = "README.md"
requires-python = ">=3.11"
license = "GPL-3.0"
keywords = []
authors = [
  { name = "Ion Reguera", email = "ion@stagelab.com" },
  { name = "Adrià Masip", email = "adria.back@gmail.com" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = [
    "cuemsutils==0.0.7.post1",
    "mido==1.3.3",
    "pyossia @ file://../libossia/build/src/ossia-python/dist/pyossia-2.0.0rc4+141.gd0976a683-cp311-cp311-linux_x86_64.whl",
    "python-osc==1.9.3",
    "rtmidi==2.5.0",
    "zeroconf==0.146.1",
]

[project.urls]
Documentation = "https://github.com/stagesoft/cuems-engine#readme"
Issues = "https://github.com/stagesoft/cuems-engine/issues"
Source = "https://github.com/stagesoft/cuems-engine"

[tool.hatch.version]
path = "src/cuemsengine/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/cuemsengine"]

[[tool.hatch.envs.hatch-test.matrix]]
python = ["3.11"]

[tool.hatch.envs.test]
dependencies = [
  "pytest",
  "pytest-cov",
  "pytest-mock",
  "coverage[toml]"
]
installer = "pip"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-v"

[tool.hatch.metadata]
allow-direct-references = true

[tool.coverage.run]
source_pkgs = ["cuemsengine", "tests"]
branch = true
concurrency = ["multiprocessing"]
omit = []

[tool.coverage.paths]
cuemsengine = ["src/cuemsengine", "*/cuems-engine/src/cuemsengine"]
tests = ["tests", "*/cuems-engine/tests"]

[tool.coverage.report]
show_missing = true
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
]

[tool.hatch.envs.types]
extra-dependencies = [
  "mypy>=1.0.0"
]

[tool.hatch.envs.types.scripts]
check = "mypy --install-types --non-interactive {args:src/cuemsengine tests}"
